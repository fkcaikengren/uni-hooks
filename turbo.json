{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": [
        "dist/**"
      ]
    },
    "//#docs:build": {
      "outputs": [
        "packages/.vitepress/dist/**"
      ]
    }
  }
}
